[build]
buildCommand = "python manage.py collectstatic --noinput"

[deploy]
startCommand = "gunicorn gestao_cme.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/healthz/"
preDeployCommand = "python manage.py migrate --noinput"
